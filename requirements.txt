# Runtime dependencies (validated with Python 3.11).
# Keep the ranges bounded so a new major release does not silently change the
# numerical results of the LiDAR pipeline.
numpy>=1.26,<3
open3d>=0.19,<0.20
nicegui>=3,<4

# Optional: enables NiceGUI's native desktop window backend.  The web UI also
# works without it, so install this separately when desktop packaging is
# needed:
# pywebview>=5.4,<7
